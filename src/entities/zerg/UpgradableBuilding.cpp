#include "entities/zerg/UpgradableBuilding.hpp"
#include "GameState.hpp"

/*
template <EntityType first, EntityType second, int minerals, int gas, int time>
bool UpgradableBuilding<first, second, minerals, gas, time>::upgradeIfPossible(EntityType type, GameState &state)
{
    if (type == second)
    {
        return true;
    }
}

template <EntityType first, EntityType second, int minerals, int gas, int time>
void UpgradableBuilding<first, second, minerals, gas, time>::update(GameState &state)
{
}
*/
